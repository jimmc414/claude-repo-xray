# {PROJECT_NAME}: Developer Warm Start

> Context-efficient onboarding guide for AI programmers.
> Generated: {TIMESTAMP}
> Token budget: {TOKEN_BUDGET}

---

## 1. System Context

### Architecture View
{MERMAID_DIAGRAM}

### Workflow-Centric View (Optional)
{WORKFLOW_MERMAID_DIAGRAM}

> Generated with: `python .claude/skills/repo-xray/scripts/dependency_graph.py {SOURCE_DIR} --mermaid`

---

## 2. Architecture Overview

{ARCHITECTURE_OVERVIEW}

---

## 3. Critical Classes

### Entry Points
{ENTRY_POINTS_TABLE}

### Core Components
{CORE_COMPONENTS}

### Data Models
{DATA_MODELS}

### Execution Layer (if present)
{EXECUTOR_CLASSES_TABLE}

---

## 4. Data Flow

```
User Input
    │
    ▼
[1] {ENTRY_POINT_CLASS}.__init__(...)
    │
    ▼
[2] {ENTRY_POINT_CLASS}.{MAIN_METHOD}(...)
    │
    ├──▶ [3] {STATE_OR_CONTEXT_SETUP}
    │
    ├──▶ [4] {CORE_PROCESSING_STEP}
    │         │
    │         └──▶ {SUB_COMPONENT}
    │
    ├──▶ [5] {VALIDATION_OR_ANALYSIS}
    │
    └──▶ [6] {OUTPUT_OR_RESULT}
              │
              ▼
         Final Results
```

---

## 5. Entry Points

### CLI Commands
{CLI_COMMANDS}

### Python API
{PYTHON_API}

### Key Imports
{KEY_IMPORTS}

---

## 6. Context Hazards

**DO NOT READ these directories/files** - they consume context without providing architectural insight:

### Large Data Directories
{HAZARD_DIRECTORIES}

### Large Files
{HAZARD_FILES}

### File Extensions to Skip
{HAZARD_EXTENSIONS}

---

## 7. Quick Verification

```bash
# Check system health (customize for your project)
{HEALTH_CHECK_COMMAND}

# Run quick sanity tests
{TEST_COMMAND}

# Verify imports
{IMPORT_VERIFICATION}
```

---

## 8. X-Ray Commands

Use these scripts to explore further without consuming full context:

```bash
# Map directory structure with token estimates
python .claude/skills/repo-xray/scripts/mapper.py {SOURCE_DIR} --summary

# Extract class/method skeletons (95% token reduction)
python .claude/skills/repo-xray/scripts/skeleton.py {SOURCE_DIR}

# Filter by priority level (critical, high, medium, low)
python .claude/skills/repo-xray/scripts/skeleton.py {SOURCE_DIR} --priority critical

# Analyze import dependencies
python .claude/skills/repo-xray/scripts/dependency_graph.py {SOURCE_DIR}

# Generate Mermaid diagram for documentation
python .claude/skills/repo-xray/scripts/dependency_graph.py {SOURCE_DIR} --mermaid

# Focus on specific area
python .claude/skills/repo-xray/scripts/dependency_graph.py {SOURCE_DIR} --focus {FOCUS_AREA}
```

### Enhanced Skeleton Features
The skeleton output includes:
- **Pydantic/dataclass fields** - `name: str = Field(...)` visible
- **Decorators** - `@dataclass`, `@property`, `@tool`
- **Global constants** - `CONFIG_VAR = "value"`
- **Line numbers** - `def method(): ...  # L42`

### Token Budget Reference
| Operation | Tokens | Use When |
|-----------|--------|----------|
| mapper.py --summary | ~500 | First exploration |
| skeleton.py (1 file) | ~200-500 | Understanding interface |
| skeleton.py --priority critical | ~5K | Core architecture |
| dependency_graph.py | ~3K | Import relationships |
| dependency_graph.py --mermaid | ~500 | Documentation diagrams |

---

## 9. Architecture Layers

### Foundation (Most Imported)
{FOUNDATION_MODULES_TABLE}

### Core
{CORE_MODULES_TABLE}

### Orchestration (Most Imports)
{ORCHESTRATION_MODULES_TABLE}

---

*This document was generated by the repo_architect agent using the repo-xray skill.*
*To refresh: `@repo_architect refresh`*
